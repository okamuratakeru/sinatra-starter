<div class="header-row">
  <a href="/memos/new" class="btn">追加</a>
</div>

<ul>
  <% @memos.each do |memo| %>
    <li>
      <a href="/memos/<%= memo['id'] %>"><%= memo['title'] %></a>
    </li>
  <% end %>
</ul>
